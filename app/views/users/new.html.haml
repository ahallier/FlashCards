%h1 Create New User

= form_tag users_path do

  = label :user, :user_id, 'Email'
  = text_field :user, :email, :id=> 'signup_user'

  = label :users, :email, 'Password'
  = text_field :user, :password,:id=> 'signup_email'
  

  = submit_tag 'Create account', :id=> 'signup_create'
